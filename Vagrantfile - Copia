Vagrant.configure("2") do |config|
  config.vm.box = "ubuntu/bionic64"
  
  # configurando a rede no windows. Acessando o localhot:8089 no win, consguimos acessar estapágina q está no vbox linux
    #host é o hospedeiro (o sistema que permite o guest (convidado) a rodar). O guest é a máquina virtual.
	# em http://localhost:8089 vemos o serviço nginx
  config.vm.network "forwarded_port", guest: 80, host: 8089

  # Definindo IP estático
  #config.vm.network "private_network", ip: "192.168.50.4"
  
  # para pegar IP do DHCP da rede local e ficar público entre as máquinas da rede Perci
   #talvez a melhor opção para rede local (bridge no virtualbox)
  config.vm.network "public_network"
end
